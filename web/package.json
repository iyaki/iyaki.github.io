{
  "name": "iyaki.ar",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "publish": "rm -rf ./_site/ && npm run build && cp -r ./_site/* ../docs/"
  },
  "author": "iyaki",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "cssnano": "^6.0.1",
    "html-minifier-terser": "^7.2.0",
    "minify-xml": "^4.3.0",
    "postcss": "^8.4.32",
    "postcss-html": "^1.5.0",
    "postcss-preset-env": "^9.3.0"
  }
}
