{
  "name": "iyaki.ar",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "publish": "npm run build && cp -r ./_site/* ../docs/"
  },
  "author": "iyaki",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
